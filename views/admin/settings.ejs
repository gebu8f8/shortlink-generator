<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>系統設定 - <%= siteName %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background-color: #f8f9fa; }
        .card { border: none; border-radius: 10px; box-shadow: 0 0 15px rgba(0,0,0,0.05); }
    </style>
</head>
<body>
    <%- include('partials/header') %>

    <div class="container mt-4">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">系統設定</h5>
            </div>
            <div class="card-body">
                <form action="/admin/settings/update" method="POST">
                    <div class="mb-3">
                        <label for="siteName" class="form-label">網站名稱</label>
                        <input type="text" class="form-control" id="siteName" name="siteName" value="<%= settings.siteName %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="baseUrl" class="form-label">基礎URL</label>
                        <input type="url" class="form-control" id="baseUrl" name="baseUrl" value="<%= settings.baseUrl %>" required>
                        <div class="form-text">這是預設的短網址網域。</div>
                    </div>
                    <hr>
                    <h5 class="mt-4">驗證碼設定</h5>
                    <div class="mb-3 form-check">
                        <input type="checkbox" class="form-check-input" id="captcha_enabled" name="captcha_enabled" <%= settings.captcha.enabled ? 'checked' : '' %>>
                        <label class="form-check-label" for="captcha_enabled">啟用驗證碼</label>
                    </div>
                    <div class="mb-3">
                        <label for="captcha_type" class="form-label">驗證碼類型</label>
                        <select class="form-select" id="captcha_type" name="captcha_type">
                            <option value="alphanumeric" <%= settings.captcha.type === 'alphanumeric' ? 'selected' : '' %>>數字和英文</option>
                            <option value="numeric" <%= settings.captcha.type === 'numeric' ? 'selected' : '' %>>僅數字</option>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label for="captcha_length" class="form-label">驗證碼長度</label>
                        <input type="number" class="form-control" id="captcha_length" name="captcha_length" value="<%= settings.captcha.length %>" min="4" max="8">
                    </div>
                    <button type="submit" class="btn btn-primary">儲存設定</button>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
